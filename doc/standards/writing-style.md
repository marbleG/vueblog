# 写作规范

## Frontmatter

```yaml
---
title: 文章标题
date: 2026-07-04
publish: true
---
```

## 图片

- 使用相对路径，如 `./image/diagram.png`。
- 图片放在文章同目录的 `image/` 子目录中。

## 链接

- 内部目录链接以 `/` 结尾，如 `/database/`。
- 避免使用绝对 URL 引用站内内容。

## 代码块

- 必须指定语言，便于语法高亮。
