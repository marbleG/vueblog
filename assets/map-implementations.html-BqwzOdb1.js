import{_ as s,c as n,a as t,o as e}from"./app-ZssbyP6J.js";const p={};function l(o,a){return e(),n("div",null,[...a[0]||(a[0]=[t(`<h1 id="map-实现类-hashmap-原理" tabindex="-1"><a class="header-anchor" href="#map-实现类-hashmap-原理"><span>Map 实现类：HashMap 原理</span></a></h1><h2 id="hashmap-底层结构" tabindex="-1"><a class="header-anchor" href="#hashmap-底层结构"><span>HashMap 底层结构</span></a></h2><p>JDK 8 之后：<strong>数组 + 链表 + 红黑树</strong></p><ul><li><strong>数组</strong>：称为哈希桶数组 <code>Node&lt;K,V&gt;[] table</code>，每个位置是一个链表/红黑树</li><li><strong>链表</strong>：当多个 key hash 到同一个位置，用链表存储</li><li><strong>红黑树</strong>：当链表长度超过阈值（TREEIFY_THRESHOLD = 8），并且数组长度 &gt;= 64，链表转红黑树，提高查询性能</li></ul><h2 id="hash-算法与索引计算" tabindex="-1"><a class="header-anchor" href="#hash-算法与索引计算"><span>hash 算法与索引计算</span></a></h2><p>计算 hash 和数组索引：</p><div class="language-java line-numbers-mode" data-highlighter="prismjs" data-ext="java"><pre><code class="language-java"><span class="line"><span class="token comment">// 扰动函数，减少哈希冲突</span></span>
<span class="line"><span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token function">hash</span><span class="token punctuation">(</span><span class="token class-name">Object</span> key<span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">int</span> h<span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">return</span> <span class="token punctuation">(</span>key <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token operator">?</span> <span class="token number">0</span> <span class="token operator">:</span> <span class="token punctuation">(</span>h <span class="token operator">=</span> key<span class="token punctuation">.</span><span class="token function">hashCode</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token operator">^</span> <span class="token punctuation">(</span>h <span class="token operator">&gt;&gt;&gt;</span> <span class="token number">16</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 计算数组索引，因为长度是 2 的幂，用 &amp; 代替模运算，更快</span></span>
<span class="line"><span class="token keyword">int</span> index <span class="token operator">=</span> hash <span class="token operator">&amp;</span> <span class="token punctuation">(</span>table<span class="token punctuation">.</span>length <span class="token operator">-</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>为什么要扰动？</strong> 让高位也参与哈希，减少低位相同导致的哈希冲突。</p><p><strong>为什么长度是 2 的幂？</strong> 保证 <code>hash &amp; (length - 1)</code> 得到合法索引，并且分布均匀，减少冲突。</p><h2 id="put-流程" tabindex="-1"><a class="header-anchor" href="#put-流程"><span>put 流程</span></a></h2><ol><li>计算 key 的 hash</li><li>计算数组索引</li><li>如果数组为空，先扩容初始化数组</li><li>如果位置为空，新建节点放入，结束</li><li>如果位置不为空： <ul><li>key 相同（hash 相同且 equals 相同），覆盖旧值</li><li>如果是红黑树节点，放入红黑树</li><li>如果是链表，遍历链表，找到就覆盖，没找到就追加到链表末尾</li></ul></li><li>追加到链表后，如果链表长度 &gt; 8，并且数组长度 &gt;= 64，转红黑树 7-插入完成后，检查 size 是否超过阈值（capacity * loadFactor），超过就扩容</li></ol><h2 id="扩容机制" tabindex="-1"><a class="header-anchor" href="#扩容机制"><span>扩容机制</span></a></h2><ul><li>默认初始容量 16（<code>DEFAULT_INITIAL_CAPACITY = 1 &lt;&lt; 4</code>）</li><li>默认负载因子 0.75（<code>DEFAULT_LOAD_FACTOR = 0.75f</code>）</li><li>扩容的时候，容量翻倍，<code>newCap = oldCap &lt;&lt; 1</code></li><li>扩容后需要重新计算每个元素的索引，因为长度变了，<code>hash &amp; (newCap - 1)</code> 索引要么不变，要么增加 <code>oldCap</code></li><li>这个特性可以简化重新哈希，不需要每个都重新计算 hash</li></ul><h2 id="什么时候链表转红黑树" tabindex="-1"><a class="header-anchor" href="#什么时候链表转红黑树"><span>什么时候链表转红黑树？</span></a></h2><p>两个条件都满足：</p><ol><li>链表长度 &gt; 8（TREEIFY_THRESHOLD = 8）</li><li>数组容量 &gt;= 64（MIN_TREEIFY_CAPACITY = 64）</li></ol><p>如果数组容量 &lt; 64，只是扩容，不转树。</p><h2 id="hashmap-vs-hashtable-vs-concurrenthashmap" tabindex="-1"><a class="header-anchor" href="#hashmap-vs-hashtable-vs-concurrenthashmap"><span>HashMap vs Hashtable vs ConcurrentHashMap</span></a></h2><table><thead><tr><th>特性</th><th>HashMap</th><th>Hashtable</th><th>ConcurrentHashMap</th></tr></thead><tbody><tr><td>线程安全</td><td>❌ 不安全</td><td>✅ 安全，所有方法加 synchronized</td><td>✅ 安全，分段锁 / CAS + synchronized</td></tr><tr><td>允许 null key</td><td>✅ 允许一个 null</td><td>❌ 不允许</td><td>❌ 不允许</td></tr><tr><td>性能</td><td>高，单线程</td><td>低，全表锁</td><td>中高，并发比 Hashtable 好很多</td></tr><tr><td>推荐使用</td><td>单线程环境</td><td>不推荐，过时</td><td>并发环境</td></tr></tbody></table><h2 id="linkedhashmap" tabindex="-1"><a class="header-anchor" href="#linkedhashmap"><span>LinkedHashMap</span></a></h2><p>继承 HashMap，额外维护一个双向链表保存插入顺序：</p><ul><li>默认按插入顺序遍历</li><li>可以开启按访问顺序排序，可以实现 LRU 缓存</li><li>性能比 HashMap 略低一点，因为维护链表</li><li>使用场景：LRU 缓存，需要按照插入顺序遍历</li></ul><div class="language-java line-numbers-mode" data-highlighter="prismjs" data-ext="java"><pre><code class="language-java"><span class="line"><span class="token comment">// 开启访问顺序，最久没访问的会被移到末尾</span></span>
<span class="line"><span class="token keyword">new</span> <span class="token class-name">LinkedHashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token number">16</span><span class="token punctuation">,</span> <span class="token number">0.75f</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="treemap" tabindex="-1"><a class="header-anchor" href="#treemap"><span>TreeMap</span></a></h2><p>基于红黑树实现：</p><ul><li>key 有序，按照自然顺序或者自定义 Comparator 排序</li><li>get/put/remove 都是 O(log n)</li><li>支持范围查询：<code>subMap(fromKey, toKey)</code></li><li>key 需要实现 Comparable 接口，或者传入 Comparator</li><li>使用场景：需要 key 有序的场景</li></ul><h2 id="总结选择" tabindex="-1"><a class="header-anchor" href="#总结选择"><span>总结选择</span></a></h2><table><thead><tr><th>需求</th><th>选择</th></tr></thead><tbody><tr><td>普通 key-value 查询，不需要排序</td><td>HashMap</td></tr><tr><td>需要 key 有序，支持范围查询</td><td>TreeMap</td></tr><tr><td>需要保存插入顺序</td><td>LinkedHashMap</td></tr><tr><td>并发多线程环境</td><td>ConcurrentHashMap</td></tr></tbody></table><hr><p>参考：<a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html" target="_blank" rel="noopener noreferrer">Java API - HashMap Doc</a></p>`,30)])])}const c=s(p,[["render",l]]),r=JSON.parse('{"path":"/zh/java/collections/map-implementations.html","title":"Map 实现类：HashMap 原理","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"zh/java/collections/map-implementations.md"}');export{c as comp,r as data};
